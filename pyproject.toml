[project]
name = "achievement-lab"
version = "1.0.0"
description = "Isolated, reproducible GitHub profile-achievement experiments"
requires-python = ">=3.10"
license = {text = "MIT"}

[tool.ruff]
line-length = 100
target-version = "py310"
